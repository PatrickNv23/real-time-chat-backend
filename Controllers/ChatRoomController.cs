using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.SignalR;
using real_time_chat_backend.Implementations;
using real_time_chat_backend.Interfaces;

namespace real_time_chat_backend.Controllers;

public class ChatRoomController : ApiController
{
    /*
    [HttpPost("JoinGroup")]
    public async Task JoinGroup(string groupName)
    {
        //await context.Groups.AddToGroupAsync(context, groupName);
    }

    [HttpPost("SendMessageToGroup")]
    public async Task SendMessageToGroup(IHubContext<ChatHub> context, string groupName, string message)
    {
        
    }*/
}